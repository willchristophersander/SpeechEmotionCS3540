# Core ML/AI Libraries
# Note: PyTorch is installed separately in Dockerfile (CPU-only version)
# torch>=1.12.0  # Installed separately as CPU-only in Dockerfile
librosa>=0.10.0
numpy>=1.21.0
scipy>=1.7.0

# Web Framework
flask>=2.0.0
flask-cors>=3.0.0
gunicorn>=20.0.0

# Audio Processing
soundfile>=0.10.0
noisereduce>=2.0.0

# Utilities (removed tqdm - not needed for deployment)

